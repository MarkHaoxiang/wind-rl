# wind-rl

The `wind_rl` package: multi-agent wind-farm layout co-design (flow-map
generators, equivariant architectures, per-site fine-tuning). See the repo
root `README.md` and `docs/architecture.md` for the project-level package map.
